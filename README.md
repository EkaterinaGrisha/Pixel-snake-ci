# Snake Game 🐍: Классика на Python и Pygame

-----

## 🖼️ Игровой процесс

Игра "Змейка" в стиле ретро: управляйте змейкой с помощью стрелок, ешьте еду, чтобы расти, и избегайте столкновений со стенами или собственным телом.

-----

## 🎯 Цели и возможности проекта

### 1\. ⚙️ Основные особенности

  * **Чистое ООП:** Логика игры (модель) строго отделена от графического интерфейса (GUI).
  * **Движение и Рост:** Корректная механика движения змейки, столкновений и увеличения длины.
  * **Настраиваемость:** Легко изменить размер клетки (`CELL`), скорость игры (FPS) и цветовую схему.
  * **Assets:** Использование PNG-изображений для головы, тела и еды для пиксельной эстетики.

### 2\. 🧪 Юнит-тестирование

  * **Покрытие:** Включены тесты для проверки ключевых сценариев: инициализация, рост, смена направления и все типы столкновений.
  * **Автоматизация:** Настроен **GitHub Actions** для автоматического запуска тестов при каждом пуше или Pull Request.

-----

## 📁 Структура проекта

```
.
├── .github/
│   └── workflows/
│       └── tests.yml            # Автотесты GitHub Actions
├── assets/
│   ├── snake_head.png           # Изображение головы змейки
│   ├── snake_body.png           # Изображение тела змейки
│   └── food.png                 # Изображение еды
├── src/
│   ├── game.py                  # 🐍 Модель игры: логика змейки, столкновения
│   └── gui.py                   # 🎨 GUI на Pygame: отрисовка, обработка ввода
├── tests/
│   └── test_game.py             # Юнит-тесты модели змейки
├── LICENSE                      # Лицензия проекта
├── README.md                    # Этот файл
└── requirements.txt             # Зависимости (pygame, pillow)
```

-----

## 🚀 Установка и запуск (Для новичков)

Чтобы запустить игру, вам потребуется **Python 3.10** или выше.

### Шаг 1: Клонирование репозитория

Откройте терминал или командную строку и выполните команду:

```bash
git clone <URL_репозитория>
cd <имя_папки>
```

### Шаг 2: Создание и активация виртуального окружения

Настоятельно рекомендуется использовать виртуальное окружение, чтобы избежать конфликтов с другими проектами.

```bash
# Создаем окружение
python -m venv venv

# Активируем его
# Для Linux/Mac
source venv/bin/activate
# Для Windows
venv\Scripts\activate
```

### Шаг 3: Установка зависимостей

Установите необходимые библиотеки, включая `pygame` и `pillow`, из файла `requirements.txt`:

```bash
pip install -r requirements.txt
```

### Шаг 4: Запуск игры

Выполните команду для запуска графического интерфейса:

```bash
python src/gui.py
```

> **Управление:** Используйте клавиши со **стрелками (↑ ↓ ← →)** для управления змейкой.

-----

## 🧪 Запуск юнит-тестов

Проверьте, что вся логика игры работает корректно, запустив тесты из корня проекта:

```bash
python -m unittest discover -s tests -v
```

### ⚡ Автоматизация через GitHub Actions

Файл `.github/workflows/tests.yml` гарантирует, что тесты запускаются автоматически на **Python 3.10, 3.11 и 3.12** при каждом:

  * Пуше в ветку `main`.
  * Создании Pull Request в ветку `main`.

-----

## 🛠 Настройки игры

Все основные параметры легко настраиваются в файлах `src/game.py` и `src/gui.py`.

| Параметр | Файл | Описание |
| :--- | :--- | :--- |
| `CELL` | `src/gui.py` | Размер одной клетки в пикселях (по умолчанию: **24**). |
| `MARGIN` | `src/gui.py` | Отступ между клетками (по умолчанию: **2**). |
| `clock.tick(FPS)` | `src/gui.py` | Скорость игры (кадры в секунду; по умолчанию: **6**). |
| `width`, `height` | `src/game.py` | Размер игрового поля в клетках. |

-----
