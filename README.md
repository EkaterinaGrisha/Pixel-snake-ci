```markdown
# Snake Game 🐍

Простая, но наглядная реализация классической игры "Змейка" на Python с графическим интерфейсом на Pygame и системой юнит-тестов.  
Проект демонстрирует основы объектно-ориентированного программирования, работу с событиями и управление игровым состоянием.

---

## 📁 Структура проекта

```

.
├── .github/
│   └── workflows/
│       └── tests.yml       # Автотесты GitHub Actions для проверки кода
├── assets/
│   ├── snake_head.png      # Изображение головы змейки
│   ├── snake_body.png      # Изображение тела змейки
│   └── food.png            # Изображение еды
├── src/
│   ├── game.py             # Модель игры: логика змейки, движение, столкновения, генерация еды
│   └── gui.py              # GUI на Pygame: отрисовка змейки и еды, обработка ввода
├── tests/
│   └── test_game.py        # Юнит-тесты модели змейки
├── LICENSE                 # Лицензия проекта
├── README.md               # Этот файл
└── requirements.txt        # Зависимости проекта (pygame, pillow)

````

---

## 🎯 Цели проекта

- Создать чистую объектно-ориентированную модель игры (`SnakeModel`)  
- Реализовать интерактивный GUI с Pygame  
- Поддержка юнит-тестов для проверки основных сценариев игры  
- Автоматическое тестирование через GitHub Actions при пуше и PR  

---

## ⚙️ Особенности и возможности

### 1. Логика игры (`src/game.py`)

- Класс `SnakeModel` управляет состоянием змейки:
  - Позиции змейки и еды  
  - Движение в четырех направлениях  
  - Проверка столкновений со стенами и телом змейки  
  - Рост при поедании еды  
- Простая система генерации еды в свободной клетке игрового поля  

### 2. Графический интерфейс (`src/gui.py`)

- Пиксельная отрисовка змейки и еды  
- Управление стрелками: ↑ ↓ ← →  
- Реакция на столкновения: вывод сообщения `Game over` и завершение игры  
- Настройки:
  - Размер клетки (`CELL = 24`)  
  - Отступы между клетками (`MARGIN = 2`)  
  - Цвет фона (`BG = (25, 25, 30)`)  
  - Частота обновления экрана (`clock.tick(6)` FPS)  

### 3. Юнит-тесты (`tests/test_game.py`)

- Проверка корректного создания начального состояния змейки и еды  
- Проверка роста змейки при съедании еды  
- Проверка столкновений со стенами и с телом змейки  
- Тестирование смены направления движения  

---

## 🚀 Установка и запуск

1. Клонируем репозиторий:

```bash
git clone <URL_репозитория>
cd <имя_папки>
````

2. Создаем и активируем виртуальное окружение:

```bash
python -m venv venv
source venv/bin/activate  # Linux/Mac
venv\Scripts\activate     # Windows
```

3. Устанавливаем зависимости:

```bash
pip install -r requirements.txt
```

4. Запуск игры:

```bash
python src/gui.py
```

5. Запуск юнит-тестов:

```bash
python -m unittest discover -s tests -v
```

---

## ⚡ GitHub Actions

Workflow `.github/workflows/tests.yml` автоматически выполняет юнит-тесты:

* При пуше в ветку `main`
* При создании Pull Request в `main`
* Проверка на Python 3.10, 3.11 и 3.12

Пример запуска тестов:

```yaml
- name: Run unit tests
  run: python -m unittest discover -s tests -v
```

---

## 🎨 Assets

* `snake_head.png` – изображение головы змейки
* `snake_body.png` – изображение тела змейки
* `food.png` – изображение еды

Все изображения масштабируются автоматически под размер клетки (`CELL`) в игровом окне.

---

## 🛠 Настройки игры

* Размер поля и начальная длина змейки настраиваются через конструктор `SnakeModel(width, height, init_length)`
* Размер клетки (`CELL`) и отступы (`MARGIN`) можно изменить для визуального эффекта
* FPS можно изменить через `clock.tick(<значение>)` для ускорения или замедления игры

---

## 🧪 Тесты

Тестируются следующие сценарии:

* Инициализация змейки и генерация еды
* Рост змейки после поедания еды
* Столкновение со стенами
* Столкновение с телом змейки
* Корректная смена направления движения

---

## 💻 Требования

* Python >= 3.10
* Pygame >= 2.1
* Pillow

---
